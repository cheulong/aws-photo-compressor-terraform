variables:
  TF_ROOT: "terraform"
  TF_VERSION: "1.12.0"
  TF_BACKEND_CONFIG: "-backend-config=bucket=my-bucket -backend-config=region=us-east-1"

before_script:
  - apk add --no-cache curl unzip
  - curl -s -o terraform.zip https://releases.hashicorp.com/terraform/${TF_VERSION}/terraform_${TF_VERSION}_linux_amd64.zip
  - unzip terraform.zip && mv terraform /usr/local/bin/
  - terraform --version


# image:
#   name: registry.gitlab.com/gitlab-org/gitlab-build-images:terraform
#   entrypoint:
#     - '/usr/bin/env'
#     - 'PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin'

# variables:
#   TF_VAR_gitlab_token: ${GITLAB_ACCESS_TOKEN}
#   AWS_ACCESS_KEY_ID: ${MY_AWS_KEY}
#   AWS_SECRET_ACCESS_KEY : ${MY_AWS_ACCESS_KEY}
#   AWS_DEFAULT_REGION: "us-east-1"

# cache:
#   paths:
#     - .terraform

# before_script:
#   - terraform --version
#   - terraform init -backend-config="tfstate.config"

# stages:
#   - validate
#   - plan
#   - apply
#   - destroy

# validate:
#   stage: validate
#   script:
#     - terraform validate

# plan:
#   stage: plan
#   script:
#     - terraform plan -out="planfile"
#   dependencies:
#     - validate
#   artifacts:
#     paths:
#       - planfile

# apply:
#   stage: apply
#   script:
#     - terraform apply -input=false "planfile"
#   dependencies:
#     - plan
#   when: manual

# destroy: 
#   stage: destroy
#   script:
#     - terraform destroy --auto-approve
#   when: manual 